[build]
packageManager = "pnpm"
buildCommand = "pnpm run -r --if-present build"

[start]
command = "node artifacts/api-server/db/migrate.mjs && node --enable-source-maps artifacts/api-server/dist/index.mjs"
